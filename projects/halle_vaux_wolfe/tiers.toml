# No suprasegmental tiers — the HVW geometry as drawn is purely segmental. An empty tiers
# file overrides the shipped default's tone/stress tiers (which reference features this
# system does not define).
